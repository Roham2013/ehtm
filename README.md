# ehtm
EHTM (Easy HyperText Markup Language) is a simple yet powerful markup language that compiles to standard HTML. It ships as a single standalone executable (ehtm.exe) requiring no external dependencies.
